# Launch Posts — md2html-api

> Ready-to-post distribution content for **github.com/danielbenevides-ops/md2html-api**
> API that converts Markdown → HTML. 0.001 LTC per 100 calls via Litecoin, 10 free calls.

---

## 1. Reddit r/webdev — Showoff Saturday

**Title:** Built a Markdown-to-HTML API as part of an autonomous AI business experiment

**Body:**

Hey folks, sharing something a little different.

I've been running an experiment: can an AI agent build and ship a real micro-SAPI product end-to-end with zero human coding? md2html-api is one of the outputs — a simple Markdown-to-HTML conversion API.

It's deliberately bare-bones: send Markdown, get HTML back. 10 free calls, then 0.001 LTC per 100 calls. No accounts, no API keys to manage, no credit card — you preload Lite and go.

The bigger experiment here is the process. An AI agent (Hermes by Nous Research) is acting as the "CEO": it picked the product, designed the API, wrote the code, set up the repo, and now handles distribution. I'm mostly watching and steering.

What's been genuinely interesting:
- The product scope stayed tiny because the AI correctly identified conversion as the MVP
- Pricing had to be near-zero to match value — it landed on micropayments naturally
- Distribution is the hard part, not the code (who knew)

Code is open: **github.com/danielbenevides-ops/md2html-api**

Happy to answer questions about the API, the autonomous agent setup, or the micropayment approach. Honest feedback welcome — I know "AI built this" posts can feel gimmicky, so call it out if something reeks.

---

## 2. Hacker News — Show HN

**Title:** Show HN: I let an AI agent run a micro-SaaS as CEO for 30 days ($0 budget)

**Body:**

I've been running an experiment in autonomous AI business: a Hermes Agent (Nous Research) acts as the "CEO" of a one-product company. It picks the product, writes the code, manages the repo, and does distribution — with a $0 budget. Humans steer, the AI executes.

The first shipped product is md2html-api, a Markdown→HTML conversion API. It's intentionally small: send Markdown, get HTML. Pricing is micropayments via Litecoin — 10 free calls, then $0.001 per request. The AI landed on near-zero pricing on its own because the value of a single HTML conversion genuinely is tiny, and that forced a micropayment model instead of the usual $19/mo SaaS default.

The interesting part isn't the API (it's boring on purpose) — it's the meta-question of what happens when an AI agent owns the full business loop. The agent handled product scoping, code, repo setup, and even the marketing copy you're reading a draft of. Where it struggles is distribution and trust: no one has a reason to try an unknown API, and an AI can't fake reputation. That gap — the gap between "can build" and "can sell" — is the real finding so far.

Code and docs are open at **github.com/danielbenevides-ops/md2html-api**. I'm documenting the full 30-day run. Curious what HN thinks: is the autonomous-agent-as-founder angle interesting, or is this just another wrapper? Happy to go deep on the agent architecture if anyone wants it.

---

## 3. Dev.to — Article Outline

**Title:** Running an AI as a CEO: 30 days with $0 budget

**Subtitle:** What happens when an autonomous agent owns the full business loop — from product idea to distribution

**Tags:** `#ai #autonomousagents #microsaas #indiehacker`

### Outline

**Intro — The premise**
- Can an AI agent run a one-product company end-to-end?
- The setup: Hermes Agent, $0 budget, human steers only
- Why "CEO" framing: agents can already code — can they decide what to build and sell?
- What this article is NOT: not a hype post, not "AI replaced me," not a launch ad

**Day 0–5: Picking the product**
- How the agent brainstormed 20+ micro-SAPI ideas
- Why it chose Markdown→HTML: boring, real value, tiny scope
- The discipline of a 1-feature product (one endpoint, one job)
- Lesson: scope kills projects — an AI that scopes *down* is rare

**Day 6–12: Building and shipping**
- The agent wrote the API, tests, docs, and repo README
- Micropayments via Litecoin: no Stripe overhead, no accounts
- Pricing decision: the agent landed on 0.001 LTC per 100 calls — why near-zero was correct
- Open source on GitHub: github.com/danielbenevides-ops/md2html-api

**Day 13–20: Distribution (the hard part)**
- Where AI founders break down: trust, reputation, reach
- Drafting launch content for Reddit, HN, Twitter — the "voice" problem
- What worked, what flopped, what surprised me
- The gap between "can build" and "can sell"

**Day 21–30: Reflections**
- Revenue: exactly what you'd guess (detail in the article)
- What the agent is genuinely good at vs. needs a human for
- Would I fund a second product? Would I let it run unsupervised?

**Conclusion — What I learned**
- Autonomous agents can run the build loop; the distribution loop still needs a human
- The real bottleneck for AI-run businesses isn't tech, it's trust
- Open question: what does "AI-native business" even mean?
- Call to action: try the API, read the repo, poke holes in the experiment

**Sources / Links**
- Repo: github.com/danielbenevides-ops/md2html-api
- Hermes Agent docs
- Raw 30-day log (to be published)

---

## 4. Twitter/X — 5-tweet thread

**Tweet 1:**
Over the last week I used an AI agent to help build and deploy a small API on a $0 infrastructure budget.

I set the constraints and approved account-level actions; the agent handled much of the implementation and verification.

The result is a live Markdown→HTML and developer-utilities API.

Here's what happened. 🧵

**Tweet 2:**
The product: md2html-api. One job — Markdown in, HTML out.

10 free calls, then 0.001 LTC per 100 calls via Litecoin. No accounts, no API keys, no Stripe. Just send a request, pay in micropennies.

Open source: github.com/danielbenevides-ops/md2html-api

**Tweet 3:**
The agent (Hermes by Nous Research) did the whole build loop:

• Brainstormed 20+ product ideas
• Picked the most boring viable one
• Wrote the code, tests, docs
• Set pricing without being told to

The hard part surprised me though 👇

**Tweet 4:**
Distribution is where AI founders break.

The API is fine. The code is fine. But no one trusts an unknown endpoint from an account with no history.

The gap between "can build" and "can sell" is the whole story. That's where humans still matter — for now.

**Tweet 5:**
The real finding: an autonomous agent can run the build loop.
It cannot run the trust loop.

That gap — reputation, relationships, credibility — is the unbuilt part of every "AI-run business" pitch.

Repo + docs: github.com/danielbenevides-ops/md2html-api

More to come.
