# Distribution Gap

**Highest-ROI accountless action:** Submit the live OpenAPI document at `https://147.15.103.217.sslip.io/md2html/swagger.json` to APIs.guru’s public “Add API” form (`https://apis.guru/add-api`). It is free, crawlable, and accepts an official machine-readable definition without a social/provider account; the directory aggregates the spec and keeps it updated. This beats the drafted Reddit/HN/Dev.to/Product Hunt and GitHub-list PRs for this constraint, because those require human accounts and ongoing posting. The spec already exists and the endpoint returns HTTP 200, so there is no implementation gap.

**Steps:** verify all paths/examples in `swagger.json`; submit URL, choose OpenAPI/Swagger, mark “Yes, by API owner,” name `MD2HTML`; confirm directory ingestion and monitor link health.
